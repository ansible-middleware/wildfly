---
wildfly_systemd:
  user: "{{ wildfly_user  }}"
  group: "{{ wildfly_group }}"
  home: "{{ wildfly_home }}"
  config: "{{ wildfly_config_base }}"
  enabled: "{{ wildfly_systemd_enabled }}"

wildfly_yml_config_path: /modules/system/layers/base/org/jboss/as/controller/main/dir/META-INF/services/
