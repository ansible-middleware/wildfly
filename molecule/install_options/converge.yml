---
- import_playbook: ../../playbooks/playbook.yml
  vars:
    wildfly_install_workdir: /apps/
    wildfly_user: mware
    wildfly_group: sysgroup
    wildfly_java_package_name: "{{ 'java-1.8.0-openjdk' if wildfly_java_version|string == '1.8' else 'java-' + wildfly_java_version|string + '-openjdk-headless' }}"
